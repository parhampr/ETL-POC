# =============================================================================
# Zara ETL POC Configuration
# =============================================================================

# LLM Configuration
# Get your OpenAI API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_api_key_here
DEFAULT_MODEL=gpt-4o-mini
MAX_TOKENS=4000

# arXiv Configuration
ARXIV_MAX_RESULTS=10
ARXIV_CATEGORIES=cs.AI,cs.CL,cs.LG,cs.CV,stat.ML

# Processing Configuration
BATCH_SIZE=5
QUALITY_THRESHOLD=0.7
RETRY_COUNT=3

# Storage Configuration (don't change unless needed)
DATA_VOLUME=./data
LOGS_VOLUME=./logs

# Database Configuration (don't change unless needed)
POSTGRES_DB=airflow
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow

# Airflow Configuration (don't change unless needed)
AIRFLOW_UID=50000
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin

# Docker Configuration
COMPOSE_PROJECT_NAME=zara-etl-poc